import { styled, Box } from '@mui/material';

export const ImageBox = styled(Box)`
    width: fit-content;
    padding: 32px;
    background: #1b1c22;
    border-radius: 8px;
    .img {
        border-radius: 8px;
    }
`;
